# 验证与交付边界

2026-09-23 独立工具修复候选：`node --test test/tool.test.cjs`：37项通过；仅本地夹具和既有脱敏R4样本，未调用新模型。

- M01：一turn三条去重usage的响应数贯通角色汇总、JSON、CSV和Markdown；重复记录不增数，且明确供应商请求总数未验证。
- M02：配置核验、计量完整、已知费用和指定范围比较资格分开；截止前未完成、累计不符、阶段费用或未知成本均不误判可比较。
- M03：响应级费用汇总保留已知小计，未知总额仍为null；Codex credits和API USD分列，缺缓存写入数据不伪造0。
- M04：公开初始题面从已核对冻结payload生成，未来消息排除；篡改公开包在复用前拒绝。独立审核R1发现初始题面若同时列为follow-up可被错误排除；修复后在工作区创建前拒绝重叠，反例核对输出目录未创建。
- M05：`A`/`B--C`与`A--B`/`C`碰撞在创建工作区前拒绝并列明两来源。
- M06：分析复用核验study/events、计算与workflow版本、JSON/CSV/Markdown身份；污染输出则旁路新目录离线重算，旧目录不覆盖。
- GPT-6合成夹具核对精确model ID、日期、API USD缓存写入费率与观测输出token/活动秒。夹具费率不代表官方或用户实际价格；未读取套餐额度差，记N/A。

- 从脱敏逐响应数据复算原R4：受试267.44061、统筹285.85545 credits。统计截点为2026-09-07T03:39:53.002Z，原未计尾部限制保留，统筹未知活动时间仍为null。
- 覆盖去重及冲突重复、缓存输入计费、reasoning不重复计、缺失费率/实际配置、错误配置仍计观测费用、明确统筹turn归属、截止时间、缺失值、不同时间定义和证据通道隔离。
- 以原R4 C2公开材料、原评分脚本及两份原首交做纯本地增量重放：重复运行新增结果为0；追加仅产生F2--C2；D8--C2所有首次文件哈希不变；原D8机械3/5失败及外部假阴性解释分开保留；费用账本输入未变，报告直接复用。
- 首交路径未提供时，摘要准确列出`first_submission`及对应job；配置缺失、费用缺失、计量累计不符或语义待判也有独立缺项，不向模型输出全会话或全量日志。
- 输入/评分/首交/机械结果漂移会报错或明确提示新ID；追加文件也参与完整性检查。未来follow-up文件不会放入初始受试包。
- 提取前记录的889个原study文件逐项校验无漂移；未运行原试验脚本写回旧结果，未改历史题面、评分、提交或冻结清单。

本工具开发费用单列：截至2026-09-07T05:13:42.759Z，当前开发回合实际Astra/high，估算至少143.08390 credits。由可选日志适配器仅选当前开发turn后计算，不含任何R4 turn；最后验证/文档/打包/回传/答复尾部未计。它是开发成本，不是后续使用成本，也没有被摊回历史试验。

该交付证明脚本能避免重复准备/检查/重写首交，并生成摘要与缺项；**没有开展真实后续模型试验，因此没有实测节省百分比或总体质量结论**。题目有效性、语义审查、验收器异常解释、宿主实际派发与最终资源选择仍需人/模型。没有调度器、服务、自动消息、自动升档、Framework安装或资源策略写入。

测试期间曾有一条错误测试把两个不同job的标签清单要求逐字节相同；已改为检查原job哈希不变及新job仅新增。没有通过修改旧试验分数来满足测试。
