# 用户发行包导航

先阅读本包的 [README](README.md) 和当前 [版本说明](framework/versions/{{FRAMEWORK_VERSION}}/README.md)。

处理已有治理项目时，从该项目根目录的 `.ai-workspace/BOOTSTRAP.md` 开始，并以项目实际 pin、任务卡、Controller 与授权包为准。首次注册项目尚无 Bootstrap 时，先按本包 README、当前版本元数据和注册预览工作；只在用户已有授权范围内应用注册，入口生成后再从项目 Bootstrap 恢复。缺少尚未生成的 Bootstrap 不构成首次注册阻塞。

从本包 [README.md](README.md) 确认用户入口、分发标识与内部版本；分发名称不授予采用资格。标准资料位置由用户选择，优先直接引用已有文档，提取或改造可选。

这个解压目录是发行包，不是 Framework 维护仓；不要寻找或假定存在维护任务、开发 runtime 或源码仓 Git 状态。

本文件只提供导航，不授予项目写入、测试、Review、Git、发布或外部操作权限。
