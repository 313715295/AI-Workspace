# {{DISPLAY_NAME}} — review profile

## Required CRITICAL review dimensions

- single dynamic authority and absence of a target control plane;
- control/target Git-top identity, sibling path safety and reparse failure;
- one-package/one-repository authorization and repo-relative exact paths;
- separate dirty, writer, Reviewer and Git closer boundaries;
- CURRENT-only steady-state recovery, unique authority and target control-plane absence;
- stable-version immutability, candidate manifest and consumer opt-in;
- explicit target AGENTS loading in Codex-hosted work.

Framework public topology, config schema, resolver, authorization, safe Git and formal release always require fresh independent CRITICAL Review. Any project-specific physical migration has its own independent Review and authorization. Reviewer receives no candidate write, Git or external authority.
