# Active task index

No task is authorized by this placeholder. Add the current controller task and any project-specific physical-migration or Framework-release task only after project facts, two Git tops, target identity and user decisions are verified. Physical migration is not a Framework runtime route.

Each active row projects task ID, path, owner, profile, role, phase and next actor. The task card's `Work route` remains authority; this index is only a locator and must not override a conflicting card.

Current writer/reviewer/authorization=`NONE / NONE / NONE`; Git/push/external=`CLOSED / CLOSED / CLOSED`.
