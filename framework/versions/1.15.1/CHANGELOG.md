# Framework 1.15.1 change log

Baseline: immutable Framework 1.15.0. Release class: `PATCH`.

## Fixed

- 1.14.0/1.14.1 adoption may cross the current-pin selected-rule-pack ceiling only when the sealed current resolver reports exactly `SELECTED_RULE_PACK_BUDGET_EXCEEDED`, the current composer independently succeeds, the complete selected rule bodies fit the 1.15.1 absolute ceiling and the existing actor-bound upgrade package and every categorical gate remain valid;
- Router context measurements derive the installed canonical Skill byte length at test time and independently enforce the canonical identity, required navigation semantics and maximum ceiling instead of repeating a frozen 4452-byte value in each context fixture.

## Preserved

- immutable Framework 1.14.1 and 1.15.0 bytes;
- the existing resolver/composer, actor-bound authorization, task-last projection, recovery, Review, `OWNER_ACCEPT`, Git/external and protected-path boundaries;
- complete selected rule text at natural context boundaries, continuous-work reuse, compact obligation foregrounding before governed actions and final delivery, and complete selected-rule reload when context is uncertain;
- PowerShell 7 on Windows as the only official/evidenced backend for this release.

## Not added

No Router trigger refactor, Skill compression, compact receipt format, temporary runner cleanup, generic consumer fast path, ROADMAP reordering, Knowledge change, new host/backend/platform behavior, service, registry, ledger, poller, automatic adoption or consumer write.
